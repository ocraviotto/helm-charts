{{- if and .Values.envFromSecret.enabled (empty .Values.envFromSecret.existingSecret) -}}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s" (tpl .Values.envFromSecret.secretName $) }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "readeck.labels" . | nindent 4 }}
    {{- with .Values.envFromSecret.secretLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.envFromSecret.secretAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{- include "readeck.secrets.data" . | nindent 2 }}
{{- end -}}
